<%= form_for([question, answer]) do |f| %>
		<%= fields_for :answer do |ff| %>
			<%= ff.label :content, 'answer' %>
			<%= ff.text_field :content %>
		<% end %>

  	<%= f.submit 'submit' %>
<% end %>