<%= form_for [item, comparison] do |f| %>
	<%= f.hidden_field :item_id, value: item.id %>
	<%= f.hidden_field :compared_id, value: compared.id %>

	<%= f.submit 'compare', class: 'check_icon' %>
<% end %>